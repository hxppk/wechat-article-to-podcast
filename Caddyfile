{
	email admin@hxppk.cn
}

podcast.hxppk.cn {
	encode gzip
	# 本地 worker 上传 mp3（multipart），放宽请求体与超时
	request_body {
		max_size 100MB
	}
	reverse_proxy app:8080 {
		transport http {
			read_timeout 30m
			write_timeout 30m
		}
	}
}
