# 变更：新增本地数据库查询 CLI

## 为什么
当前只能通过 GUI 或直接打开数据库文件查看数据，不便在终端快速检索用户与播客记录。需要一个最小可用的 CLI 工具支持直接执行 SQL 查询。

## 变更内容
- 新增一个本地 CLI 脚本，允许在终端直接执行 SQL 查询 `data/app.db`。
- 输出查询结果到标准输出，便于在 Codex/Claude 终端中查看。
- 提供清晰的使用方式与错误提示。

## 影响范围
- 影响规格：新增 `db-query-cli` 能力
- 影响代码：`scripts/`、`package.json`（新增可选脚本入口）
