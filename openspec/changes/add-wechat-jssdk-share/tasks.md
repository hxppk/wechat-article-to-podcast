## 1. 实现
- [x] 增加 JS-SDK 签名接口（含 access_token/jsapi_ticket 缓存）。
- [x] 前端在微信内加载 JS-SDK 并设置分享数据（标题/摘要/封面/链接）。
- [x] 添加 JS 接口安全域名验证文件到 `public/` 根目录。
- [x] 新增环境变量说明（AppID/AppSecret）。

## 2. 验证
- [ ] 在微信内分享链接，展示标题/摘要/封面。
- [ ] 分享链接与 og meta 仍可正常访问。
