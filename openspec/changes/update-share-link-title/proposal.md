# 变更：分享链接增加标题参数

## 为什么
当前分享链接只包含 shareId，复制后难以直观看出内容。增加文章标题可以让分享链接更易识别。

## 改什么
- 分享链接生成时追加 URL 编码后的 `title` 参数。
- 当标题为空时不追加 `title`，链接仍可正常访问。

## 影响范围
- 影响规格：share-links
- 影响代码：public/script.js（分享链接生成）
